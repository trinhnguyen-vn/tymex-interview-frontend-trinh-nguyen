import { Container, Typography } from '@mui/material';

export default function Page() {
  return (
    <Container sx={{ px: 5, py: 15 }}>
      <Typography variant="h1">Connect Wallet</Typography>
    </Container>
  );
}